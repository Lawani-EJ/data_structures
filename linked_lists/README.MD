### Linked lists 
A linked list is a data structure used for storing collection of data, it is a type of linear data structure where individual items are not at contigous locations, these contigous locations are called nodes and they are connected to each other using links.

